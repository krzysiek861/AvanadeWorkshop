trigger:
  branches:
    include:
      - feature/ado-pipelines

pool:
  vmImage: "windows-latest"

stages:
  - stage: build_an_app
    jobs:
      - job:
        displayName: Build
        steps:
        - script: echo building an app
  - stage: deploy_an_app
    jobs:
      - job:
        displayName: Deploy
        steps:
          - script: echo deploying an app